micronaut:
  application:
    name: micronaut-http-client
  server:
    cors:
      enabled: true
    router:
      static-resources:
        default:
          enabled: true
          mapping: /**
          paths: classpath:public
    codec:
      json:
        additionalTypes:
          - text/javascript